<table>
  <tr>
    <th>FQDN</th>
    <th>Overall</th>
    <th>Owner</th>
    <th>SCOM</th>
    <th>Akorri<br />Storage</th>
    <th>EPO<br />DAT</th>
    <th>EPO<br />Update</th>
    <th>VMware<br />CPU</th>
    <th>VMware<br />Memory</th>
    <th>Avamar</th>
    <th>WSUS<br />Updates</th>
  </tr>
  <% for computer in @computers %>
    <tr>
      <td><%= link_to computer.fqdn, computer %></td>
      <td class="health"><%= food_icon(computer.health) %></td>
      <td class="health"><%= computer.scom_computer ? computer.scom_computer.owner ? computer.scom_computer.owner.initials : "" : "" %>
      <td class="health"><%= computer.scom_computer ? food_icon(computer.scom_computer.health) : food_icon(4) %></td>
      <td class="health"><%= computer.akorri_server_storage ? food_icon(computer.akorri_server_storage.health) : food_icon(4) %></td>
      <td class="health"><%= computer.epo_computer ? food_icon(computer.epo_computer.dat_health) : food_icon(5) %></td>
      <td class="health"><%= computer.epo_computer ? food_icon(computer.epo_computer.update_health) : food_icon(5) %></td>
      <td class="health"><%= computer.vmware_computer ? food_icon(computer.vmware_computer.cpu_health) : food_icon(4) %></td>
      <td class="health"><%= computer.vmware_computer ? food_icon(computer.vmware_computer.memory_health) : food_icon(4) %></td>
      <td class="health"><%= computer.avamar_computer ? food_icon(computer.avamar_computer.health) : food_icon(5) %>
      <td class="health">
        <%= computer.wsus_computer ? health_number(computer.wsus_computer.updates_outstanding, computer.wsus_computer.update_health) : food_icon(5) %>
      </td>
    </tr>
  <% end %>
</table>